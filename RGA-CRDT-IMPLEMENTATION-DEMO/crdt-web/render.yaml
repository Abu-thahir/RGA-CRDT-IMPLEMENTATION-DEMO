services:
  - type: web
    name: crdt-text-editor
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run dev
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
